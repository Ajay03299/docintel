# Copy to .env and adjust. Default port is 5432; override if it conflicts
# with a local Postgres (this project's author uses 5433 for that reason).
DATABASE_URL=postgresql+psycopg://docintel:docintel_dev@localhost:5432/docintel
